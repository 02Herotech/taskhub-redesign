const userData = {};

interface ChatDataTypes {
  id: string;
  image: string;
  name: string;
  lastMessage: string;
  date: string;
  chatNo: string;
}

export const chatData: ChatDataTypes[] = [];
